/**
 * Cron jobs — register scheduled tasks here.
 * Example: cleanup expired refresh tokens daily.
 */
export function registerCronJobs(): void {
  // Implement with node-cron or similar when needed
}
